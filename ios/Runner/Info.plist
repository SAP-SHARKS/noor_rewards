<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
	<dict>
		<key>CADisableMinimumFrameDurationOnPhone</key>
		<true/>
		<key>CFBundleDevelopmentRegion</key>
		<string>$(DEVELOPMENT_LANGUAGE)</string>
		<key>CFBundleDisplayName</key>
		<string>Sabiq</string>
		<key>CFBundleExecutable</key>
		<string>$(EXECUTABLE_NAME)</string>
		<key>CFBundleIdentifier</key>
		<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
		<key>CFBundleInfoDictionaryVersion</key>
		<string>6.0</string>
		<key>CFBundleName</key>
		<string>noor_rewards</string>
		<key>CFBundlePackageType</key>
		<string>APPL</string>
		<key>CFBundleShortVersionString</key>
		<string>$(FLUTTER_BUILD_NAME)</string>
		<key>CFBundleSignature</key>
		<string>????</string>
		<key>CFBundleVersion</key>
		<string>$(FLUTTER_BUILD_NUMBER)</string>
		<key>LSRequiresIPhoneOS</key>
		<true/>
		<key>UIApplicationSceneManifest</key>
		<dict>
			<key>UIApplicationSupportsMultipleScenes</key>
			<false/>
			<key>UISceneConfigurations</key>
			<dict>
				<key>UIWindowSceneSessionRoleApplication</key>
				<array>
					<dict>
						<key>UISceneClassName</key>
						<string>UIWindowScene</string>
						<key>UISceneConfigurationName</key>
						<string>flutter</string>
						<key>UISceneDelegateClassName</key>
						<string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
						<key>UISceneStoryboardFile</key>
						<string>Main</string>
					</dict>
				</array>
			</dict>
		</dict>
		<key>UIApplicationSupportsIndirectInputEvents</key>
		<true/>
		<key>UILaunchStoryboardName</key>
		<string>LaunchScreen</string>
		<key>UIMainStoryboardFile</key>
		<string>Main</string>
		<key>UISupportedInterfaceOrientations</key>
		<array>
			<string>UIInterfaceOrientationPortrait</string>
			<string>UIInterfaceOrientationLandscapeLeft</string>
			<string>UIInterfaceOrientationLandscapeRight</string>
		</array>
		<key>UISupportedInterfaceOrientations~ipad</key>
		<array>
			<string>UIInterfaceOrientationPortrait</string>
			<string>UIInterfaceOrientationPortraitUpsideDown</string>
			<string>UIInterfaceOrientationLandscapeLeft</string>
			<string>UIInterfaceOrientationLandscapeRight</string>
		</array>
		<key>CFBundleURLTypes</key>
		<array>
			<dict>
				<key>CFBundleTypeRole</key>
				<string>Editor</string>
				<key>CFBundleURLName</key>
				<string>com.sabiq.app.supabase</string>
				<key>CFBundleURLSchemes</key>
				<array>
					<string>io.supabase.flutterquickstart</string>
				</array>
			</dict>
			<dict>
				<!-- Quran Foundation OAuth2 PKCE callback + referral deep
				     link. Mirrors the Android intent filters in
				     AndroidManifest.xml so the same noorrewards:// URLs
				     work on iOS too. Without this iOS Safari errors with
				     "Safari cannot open the page because the address is
				     invalid" after the user completes login at
				     auth.quran.com. -->
				<key>CFBundleTypeRole</key>
				<string>Editor</string>
				<key>CFBundleURLName</key>
				<string>com.sabiq.app.app</string>
				<key>CFBundleURLSchemes</key>
				<array>
					<string>noorrewards</string>
				</array>
			</dict>
			<dict>
				<!-- Google Sign-In URL scheme.
				     The GoogleSignIn SDK invokes this reversed-client-ID URL
				     to hand the OAuth response back to the app when the
				     Google account picker closes. The scheme value MUST be
				     the iOS OAuth Client ID reversed (dots reordered), e.g.
				     an ID of `123-abc.apps.googleusercontent.com` becomes
				     `com.googleusercontent.apps.123-abc`.
				     Fetch from Google Cloud Console → APIs & Services →
				     Credentials → iOS OAuth 2.0 Client → "iOS URL scheme". -->
				<key>CFBundleTypeRole</key>
				<string>Editor</string>
				<key>CFBundleURLName</key>
				<string>com.sabiq.app.googlesignin</string>
				<key>CFBundleURLSchemes</key>
				<array>
					<string>com.googleusercontent.apps.933672581282-n8elev85hl2mud9bqamlmu5t7h7h49ju</string>
				</array>
			</dict>
		</array>
		<!-- Google Sign-In needs the raw iOS OAuth Client ID (not reversed)
		     to initialize itself. Fetch from Google Cloud Console → APIs &
		     Services → Credentials → iOS OAuth 2.0 Client → "iOS client ID". -->
		<key>GIDClientID</key>
		<string>933672581282-n8elev85hl2mud9bqamlmu5t7h7h49ju.apps.googleusercontent.com</string>
		<key>UIStatusBarHidden</key>
		<false/>
		<!-- ── Privacy purpose strings (NSUsageDescription) ────────────────────
		     Required by App Store Connect whenever the binary references the
		     matching iOS API — even via a transitive plugin. Apple's
		     processing scanner rejects builds that omit these.
		     Location keys removed — timezone is now read from the device's
		     system settings via flutter_timezone, no GPS or permission
		     needed. -->
		<!-- Export Compliance: we only use standard iOS encryption (HTTPS
		     via URLSession). Declaring this here means Apple skips the
		     "Missing Compliance" prompt on every TestFlight upload — builds
		     go straight from Processing to Ready to Test. -->
		<key>ITSAppUsesNonExemptEncryption</key>
		<false/>
		<!-- Allow the app to wake briefly to process incoming FCM data
		     messages (silent pushes) so the notification handler can run
		     even when the app isn't in the foreground. -->
		<key>UIBackgroundModes</key>
		<array>
			<string>remote-notification</string>
		</array>
		<!-- Photo library / camera usage descriptions were removed with the
		     profile avatar upload feature — Sabiq now renders initials-only
		     avatars (UGC-compliance policy). No plugin in the app requests
		     Photos or Camera access, so no NSUsageDescription strings are
		     needed here; Apple's binary scanner will not flag their absence. -->
	</dict>
</plist>
